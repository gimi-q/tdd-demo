import React from 'react';
import ReactDOM from 'react-dom';
import QuizApp from './Quiz';
import getQuestions from '../helpers/helpers';

describe('Quiz', () => {
  it('fetches questions from opentdb.com', () => {
    // expect(getQuestions(10)).toEqual('test')
  });
});
